version: "3"

services:
  empyrion:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: empyrion_container
    environment:
      - DISPLAY=:0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    network_mode: host
    restart: always
